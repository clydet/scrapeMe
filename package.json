{
  "name": "scrapeTicket",
  "version": "1.0.0",
  "description": "check for ticket availability",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "clydet",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^0.19.0",
    "follow-redirects": "0.0.6",
    "request": "^2.58.0"
  }
}
